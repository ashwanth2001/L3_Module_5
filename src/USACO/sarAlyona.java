//package USACO;

//public class sarAlyona {

//}

//int main(){ long long a, k, r[5] = { 0 }, l[5]={ 0 }; cin >> a >> k; for (int i = 1; i <= a; i++) r[i % 5]++; for (int j = 1; j <= k; j++) l[j % 5]++; cout << (r[0]*l[0]+r[1]*l[4]+r[4]*l[1]+r[2]*l[3]+r[3]*l[2]) << endl; }
